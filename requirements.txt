# 🚀 Python 3.13 Compatible Requirements (Pure Python Only!)
# These packages have NO binary dependencies and work with Python 3.13!

# Core web framework - pure Python
fastapi==0.95.2

# ASGI server - pure Python
uvicorn[standard]==0.22.0

# Database adapter - pure Python, no binary dependencies
pg8000==1.29.8

# Data processing - removed pandas (not needed for main backend)
# pandas==2.2.0



# Environment management
python-dotenv==1.0.0
