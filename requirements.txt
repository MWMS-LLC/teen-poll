# Core web framework
fastapi>=0.100,<1.0

# ASGI server
uvicorn[standard]>=0.22,<1.0

# Database adapter (pure Python Postgres)
pg8000>=1.29,<2.0

# Local environment variable loader (for dev only; safe to keep)
python-dotenv>=1.0,<2.0
